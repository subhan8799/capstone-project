# next-chat (Checkpoint 1 demo)

Minimal Next.js demo implementing Checkpoint 1: streaming chat UI, error handling, retry, skeletons, empty states, responsive mobile layout, and dev modes for failure testing.

Run locally:

```bash
cd next-chat
npm install
npm run dev
```

Test modes (use Dev mode select): `normal`, `slow`, `mid`, `network`, `rate`.
